#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
  echo "ERROR: No command provided. Usage: entrypoint <command>" >&2
  exit 1
fi

case "$1" in
  main)
    exec python /app/main.py
    ;;
  *)
    echo "Unknown command: $1" >&2
    exit 1
    ;;
esac
